{
    "name": "web-shop",
    "version": "1.0.0",
    "engines": {
        "node": ">=16"
    },
    "scripts": {
        "start": "cd server/built && node server.js",
        "prebuild": "cd server && npm run build",
        "build": "cd client && npm run build"
        
    }
}